# 客户交付检查清单

交付范围：`shop-be` 后端、`shop-fe` 前端、PostgreSQL 数据库、容器编排及运维资料。

## 功能模块

- [x] 管理员密码登录、令牌刷新、退出和账号安全
- [x] 门店查询、新增、编辑和停用
- [x] 首页实时统计、趋势、库存和加工待办
- [x] 禽类品类查询、新增、编辑和删除
- [x] 库存查询、新增、调整和损耗扣减
- [x] 供应商查询、新增、编辑和删除
- [x] 采购创建、一次性收货和自动入库
- [x] POS 销售、库存校验与扣减、订单查询和状态流转
- [x] 加工任务自动生成、分配和状态推进
- [x] 会员查询、新增和编辑
- [x] 员工查询、新增、编辑、停用及新员工密码登录
- [x] 售价和加工费查询、编辑及负数校验
- [x] 所有业务数据在进程重启后保持一致

## 质量门禁

- [x] 后端 Java 17：`./gradlew clean test bootJar`
- [x] 前端：`pnpm test:production-config && pnpm build && pnpm audit --prod`
- [x] 部署静态检查：`bash scripts/smoke.sh --static`
- [x] Compose 解析：`docker compose --env-file .env.example config`
- [x] HTTP 全流程：`node scripts/http-acceptance.mjs full`
- [x] 重启持久化：使用相同 `ACCEPTANCE_MARKER` 执行 `node scripts/http-acceptance.mjs verify-marker`
- [x] 浏览器桌面端登录、导航和核心页面检查
- [x] 浏览器移动端布局检查
- [x] 最终提交后确认前后端仓库无未提交交付文件，且 `HEAD` 与 `origin/main` 一致

## 本次验收结果

- 验收日期：2026-07-14
- 后端：81 项测试通过，0 失败、0 错误、0 跳过，`bootJar` 成功
- 前端：生产配置门禁和构建通过，生产依赖 0 个已知漏洞
- HTTP：全部业务模块通过，最终持久化标记为 `FINAL2-20260714`
- 浏览器：14 个注册功能页面通过；390 x 844 移动视口无整页横向溢出，控制台无错误
- 部署：三服务 Compose 配置解析通过，静态 smoke 通过
- 环境限制：验收机器的 Docker 守护进程未运行，未在本机执行镜像构建和三容器运行 smoke；客户环境上线时必须按 `README.md` 再执行一次运行 smoke

## 客户上线前

- [ ] 将 `.env.example` 复制为 `.env` 并替换全部 `change-me` 值
- [ ] 使用至少 32 位随机 JWT 密钥，不与测试或其他环境复用
- [ ] 首次启动后验证管理员登录，并清空初始化管理员变量
- [ ] 配置 HTTPS、域名、防火墙、镜像仓库凭据和数据库备份计划
- [ ] 完成一次备份恢复演练，记录恢复点目标和恢复时间目标
- [ ] 确认监控、日志保留、告警联系人和回滚责任人

执行结果和提交版本应在最终交付记录中填写，不应使用本地测试密钥作为生产凭据。
